## 1. 规范收敛

- [ ] 1.1 修改 `chat-compact` 规格，把 `auto_compact` 的工具暴露语义改为“开启即暴露”
- [ ] 1.2 去除 spec 中“达到可见阈值后才暴露 `compact` 工具”的规范性表述，并明确预算提示不再控制工具显隐

## 2. 后续实现对齐

- [ ] 2.1 清理运行时代码中按预算判定 `compact` 工具可见性的逻辑
- [ ] 2.2 对齐相关配置说明、测试断言和文档，移除把阈值当作工具显隐条件的旧语义
