# 编码规范
- 重要函数要进行rust的文档级别注释`///` + doc-test, 公共api注释中要有使用案例 文件开头也要对当前文件进行注释`//!` 普通的new之类的函数就不需要了
- 代码中的代码如果逻辑略微复杂需要增加简单注释 如果有显示bug逻辑导致的问题添加的功能必须予以说明
- 要做到尽可能的模块化低耦合 src下面一个文件夹就可以当成一个模块了 
- 如果源码中的一个变量是为了test而存在的 需要在变量定义后面注释说明
- 关键数据和关键函数要进行日志

# 命名规范
- 所有的变量中的LLM必须大写 严禁使用Llm这种写法

# 输出规范
- 如果输出带有文件名字+行号需要在前后加上空格方便点击跳转: <space>src/llm.rs:17<space> 

# 开发文档
- openspec请用中文编写文档
- 代码中的TODO如果用户没有要求就不要做 或者做之前要询问
- model/**.md 里面的文档是当前项目组件概念、职责和核心边界
- 关键action必须添加调试日志，初始化数据库，查询memory，网络请求调用llm等等 主要是方便调试
- 如果一个函数是为了ut的helper 需要用 `#[cfg(test)]` 来标注
- 如果用户没有提出，你引入新概念新组件一定要找用户确认
- 对外新增能力接口必须确保和现有接口能力不重合，不允许对于一个能力出两个接口，需要和用户对齐确认(添加pubapi的签名+作用+场景)

# 分支规范
- `dev` 是默认开发基线，新的 feature branch 必须从 `dev` 拉出
- 后续开发默认在 `dev` 分出的 feature branch 上进行，不直接在 `master` 上开发
- 是否将 `dev` 或 feature branch 合入 `master`，由用户最终决定
- spec需要开一个对应的{xx/$spec} 分支来做 比如 `refact/session-and-thread` 

# 测试规范
- 每一个src文件都要在对应的test/目录下以相同的文件映射方式进行ut 要求考虑边界情况 特殊情况
- 如果用户提出一个bug或者问题 在修复的同时需要针对这个问题添加一个ut 来确保问题被流程杀死
- ut中要对测试的是什么情况进行注释的文字说明

# extra
- claude is revivewing what you do

# 文档管理
agent-doc/、arch/ 等均已废弃 不要当做事实参考依据
- 组件文档: model/*.md 里面定义着每一个组件的**职责+边界+概念+建模** 而不是具体细节和函数 修改需要用户同意
- 验收文档: acceptance/*.md 里面定义着各个项目的e2e验收标准(当然只是一部分 并不能完全代表全部ut)
- 能力文档: ability/*.md 定义着当前项目有哪些能力需要实现 如何实现

如果实现前遇到用户需求和文档不一致、需要先和用户对齐文档内容

如果实现后遇到实现内容和文档不一致、需要和用户确认指出不同点

!!禁止在没有允许没有对齐的情况下私自修改文档

