支持主动式记忆和被动式记忆（Active Memory 和 Passive Memory）：

1. 被动式记忆
   它可以通过被动式记忆，将记忆写入到本地的 memory 文件夹中。

2. 主动式记忆
   则是通过记忆对应的关键词和记忆内容，作为一个渐进式披露，加载到程序运行时的 system prompt 中, 后续可以通过相应的工具去读取对应的记忆。就是在遇到对应的关键词时，可以去读取对应的一个记忆情况.

